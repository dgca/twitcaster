/// <reference types="react" />
import TweetConfig from '../../types/TweetConfig';
declare function ImagesContainer({ config }: {
    config: TweetConfig;
}): JSX.Element;
export default ImagesContainer;
